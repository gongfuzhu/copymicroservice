
spring:
  cloud:
    #配置计算机名
    consul:
      discovery:
        hostname: 192.168.31.222   # 配置实例地址
  datasource:
    url: jdbc:mysql://192.168.63.130:3306/test?characterEncoding=utf8&useSSL=false
    username: test
    password: test
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDB53Dialect #重要,使用带事务的方言否则创建的表不支持事务


#logging:
#  level:
#    org:
#      springframework: trace
#debug: true
#trace: true